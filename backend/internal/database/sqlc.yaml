version: "2"

sql:
  - engine: "postgresql"
    schema: "./migrations"
    queries: "./sql/auth"
    gen:
      go:
        package: "auth"
        out: "../../internal/auth"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_db_tags: true
        rename:
          AuthUser: User # ✅ THIS WORKS

